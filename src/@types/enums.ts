export enum Key {
  Escape = 'Escape',
  Enter = 'Enter',
  Delete = 'Delete',
  Backspace = 'Backspace',
  N = 'n'
}

export enum KeyModifier {
  Control = 'ctrlKey',
  Shift = 'shiftKey',
  Alt = 'altKey',
  Meta = 'metaKey'
}
